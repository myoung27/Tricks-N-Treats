<h1>{{houses.address}}</h1>
<div class="card" style="width: 18rem;">

    <div class="card-body">
        <h5 class="card-title"> Does this house give out candy:<br> {{houses.candy}}</h5>
        <h5 class="card-title">Are there streetlights:<br> {{houses.streetlight}}</h5>
        <a href="neighborhood/{{neighborhoods.id}}" class="btn btn-warning">Return to the Houses</a>
        <br>
        <form action="/neighborhoods/{{houses.id}}/houses/{{houses.id}}?_method=DELETE" method="POST">
        <button class='btn btn-warning' type="submit"> Delete this house</button>
        </form>
        {{!-- <a href='/neighborhoods/{{houses.id}}/houses/{{houses.id}}?_method=DELETE' method="POST" class="btn btn-warning">
            Delete this house</a> --}}
        <br>
        <a href="{{houses.id}}/edit" class="btn btn-warning">Edit this House</a>
    </div>
</div>